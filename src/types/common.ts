export interface NavLinks {
  id: number;
  links: string;
  title: string;
}

export interface Logo {
  title: string;
  src: string;
  alt: string;
  link: string;
}

export interface Image {
  src: string;
  alt: string;
}
