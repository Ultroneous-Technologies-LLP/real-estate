export interface NavLinks {
  id: number;
  links: string;
  title: string;
}

export interface Logo {
  alt: string;
  link: string;
  src: string;
  title: string;
}

export interface Image {
  alt: string;
  src: string;
}
